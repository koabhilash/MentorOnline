<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#D0C4F2">

    <!-- Back Button at Top Left -->

    <!-- Main Container with Rounded Corners -->
    <ImageButton
        android:id="@+id/backButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="start"

        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Back"
        android:padding="8dp"
        android:src="@drawable/ic_back" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/container_background"
        android:elevation="8dp"
        android:orientation="vertical"

        android:padding="24dp">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"
            android:text="SAVEETHA CGPA CALCULATOR"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Input Fields -->
        <EditText
            android:id="@+id/inputS"
            style="@style/InputField"
            android:hint="Number of S Grades" />

        <EditText
            android:id="@+id/inputA"
            style="@style/InputField"
            android:layout_marginTop="10dp"
            android:hint="Number of A Grades" />

        <EditText
            android:id="@+id/inputB"
            style="@style/InputField"
            android:layout_marginTop="10dp"
            android:hint="Number of B Grades" />

        <EditText
            android:id="@+id/inputC"
            style="@style/InputField"
            android:layout_marginTop="10dp"
            android:hint="Number of C Grades" />

        <EditText
            android:id="@+id/inputD"
            style="@style/InputField"
            android:layout_marginTop="10dp"
            android:hint="Number of D Grades" />

        <EditText
            android:id="@+id/inputE"
            style="@style/InputField"
            android:layout_marginTop="10dp"
            android:hint="Number of E Grades" />

        <!-- Calculate Button -->
        <Button
            android:id="@+id/calculateButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:background="@drawable/button_background"
            android:padding="12dp"
            android:text="Calculate"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Store CGPA Button (Initially Hidden) -->
        <Button
            android:id="@+id/storeButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="15dp"
            android:background="@drawable/button_background"
            android:padding="8dp"
            android:text="Save CGPA"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Result Text -->
        <TextView
            android:id="@+id/resultText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="10dp"
            android:textColor="#000000"
            android:textSize="24sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
